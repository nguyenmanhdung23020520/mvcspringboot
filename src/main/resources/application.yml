spring:
  application:
    name: spring-boot-web-mvc

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
  server:
    port: 8080